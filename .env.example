# Leave empty when using the CRA development proxy.
REACT_APP_API_BASE_URL=

# Optional: when set, the API uses PostgreSQL instead of .data/db.json.
# Example: postgresql://user:password@host:5432/questionnaire?sslmode=require
DATABASE_URL=

# Use a long random value outside local development.
JWT_SECRET=
